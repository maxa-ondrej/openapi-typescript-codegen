{{#>isNullable~}}
{{~#equals format 'event-stream'~}}
SSE.decoder(
	{{~#if link~}}
		{{~>type link~}}
	{{~else~}}
		{{~>base~}}
	{{~/if~}}
)
{{~else~}}
D.array(
	{{~#if link~}}
		{{~>type link~}}
	{{~else~}}
		{{~>base~}}
	{{~/if~}}
)
{{~/equals~}}
{{~/isNullable}}
