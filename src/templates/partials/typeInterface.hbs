{{~#if properties~}}
{{#>isNullable}}
D.object({
	{{#each properties}}
	{{{name}}}: {{#>isRequired}}{{#if ../parent}}{{>type parent=../parent}}{{else}}{{>type}}{{/if}}{{/isRequired}},
	{{/each}}
})
{{/isNullable}}
{{~else~}}
D.createDecoder<unknown>({
	forceDecode: (data) => data,
})
{{~/if~}}
