((result) => {{#>resultMapperIsNullable}}(() => {
{{~#if properties~}}
	const obj = result;
{{#each properties}}
	obj["{{{name}}}"] = {{#>resultMapperIsRequired}}({{#if ../parent}}{{>resultMapper parent=../parent}}{{else}}{{>resultMapper}}{{/if}})(result["{{{name}}}"]){{#if isReadOnly}} as const{{/if}}{{/resultMapperIsRequired}};
{{/each}}
	return obj;
{{~else~}}
	return result;
{{~/if~}}
})(){{/resultMapperIsNullable}})
