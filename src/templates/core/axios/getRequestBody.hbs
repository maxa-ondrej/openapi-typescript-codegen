{{>functions/convertRequestBody}}


export const getRequestBody = <T extends Record<number,D.Decoder<unknown>>>(options: ApiRequestOptions<T>): unknown => {
	if (options.body) {
		return convertRequestBody(options.body);
	}
	return undefined;
};
