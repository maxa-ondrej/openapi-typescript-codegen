{{>header}}

import type { HTTPError } from './ApiError';
import type { ApiResult } from './ApiResult';
import type { ApiRequestOptions } from './ApiRequestOptions';
import type { OpenAPIConfig } from './OpenAPI';
import type { TaskEither } from 'fp-ts/lib/TaskEither';
import type * as D from '@majkit/fp-ts-schema';

export abstract class BaseHttpRequest {

	constructor(public readonly config: OpenAPIConfig) {}

	public abstract request<T extends Record<number,D.Decoder<unknown>>>(options: ApiRequestOptions): TaskEither<HTTPError, ApiResponse<T>>;
}
